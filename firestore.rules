rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /mifta_user_profiles/{uid} {
      allow read, write: if true; // In production, restrict to request.auth.uid == uid
    }
  }
}
